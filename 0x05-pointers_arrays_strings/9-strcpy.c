#include "main.h"
#include <stdio.h>

/**
 *_strcpy - it copy the string pointed to by src including the terminating
 *null byte to the buffer to by dest.
 *@dest: the destination.
 *@src: source.
 *Return: the pointer to dest.
 */
char *_strcpy(char *dest, char *src)
{
	int count = 0;

	while (count >= 0)
	{
		*(dest + count) == '\0')
		if (*(src + count) == '\0)
			break;
		count++;
	}
	return (dest);
}
