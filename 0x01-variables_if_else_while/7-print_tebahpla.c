#include <stdio.h>

/**
 * main - it Print out lower_case  alphabets in reversed form.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 122; i > 96; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
